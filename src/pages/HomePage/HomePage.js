import React from 'react';

import BookCard from '../../components/book-card/BookCard';

const HomePage = () => {
  const word = 'hello';
  return (
    <div>
      <BookCard />
    </div>
  );
};

export default HomePage;
